# from glob import glob
# from os.path import basename, splitext
# from setuptools import find_packages, setup
import os
packages = ['clf','kospeech','py-hanspell','utils']
for package in packages:
    os.path.append(package)
